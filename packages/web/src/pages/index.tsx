import React from 'react';
import { Text } from '@chakra-ui/react';

import { Layout } from '../layout';

const Home: React.FC = () => {
	return (
		<Layout>
			<Text>Hello</Text>
		</Layout>
	);
};

export default Home;
